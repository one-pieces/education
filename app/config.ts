/// <reference path='./app.d.ts' />

var config = {
    apiBasePath: '/api',
    basePath: '/cftvc',
    defaultBasePage: 'cftvc',

    production: true
};

config.production = false;

export = config;